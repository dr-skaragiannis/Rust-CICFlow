# Zenodo publish checklist — DEF CON 26 CTF dataset (Local_Computer)

## Prepare (once)
- [ ] Log in at https://zenodo.org (personal account).
- [ ] (API route only) Create token with `deposit:write` at
      https://zenodo.org/account/settings/applications/tokens/new/
      and `set ZENODO_TOKEN=<token>` in a terminal.

## Files to include
Record A — capture:
- [ ] `DEF CON 26 ctf packet captures.pcap` (52.92 GB) — **over the 50 GB
      single-file limit**: upload the pre-generated byte-parts +
      `SHA256SUMS_parts.txt` from
      `%TEMP%\opencode\defcon26\zenodo_parts\`
      (`DEF_CON_26_ctf_packet_captures.part01of03` 21.47 GB,
      `part02of03` 21.47 GB, `part03of03` 9.97 GB; reassembly verified against
      parent SHA-256 `feca600b81f02e8b…` in `zenodo/SHA256SUMS.txt`).
- [ ] `defcon26_{head,mid,tail}.pcapng` (200k-packet subsets)
      from `experiments\20260923-020154_Local_Computer\subsets\`
- [ ] `zenodo/SHA256SUMS.txt` (verbatim)

Record B — flows:
- [ ] `full.pcap_Flow.csv` (4.58 GB) from
      `C:\Users\bloodraven\AppData\Local\Temp\opencode\defcon26\full_rust_out\`
- [ ] six subset flow CSVs from
      `experiments\20260923-020154_Local_Computer\datasets\defcon26_{head,mid,tail}\`
      (Rust: `rust_0\defcon26_*.pcapng_Flow.csv` · Python reference: `python_0.csv`)
- [ ] provenance docs: `analysis.md`, `results_summary_corrected.md`, `evaluation.md`

## Metadata per record
- [ ] Upload type: **dataset** (both records); language: English.
- [ ] Title / description HTML / keywords / license **CC-BY-4.0** / access: open —
      copy verbatim from `zenodo/zenodo.json` (JSON is deposit-schema ready).
- [ ] Related identifiers: GitHub `Rust-CICFlow` as *isSupplementedBy*, the
      media.defcon.org DEF CON 26 rar as *isDerivedFrom*.
- [ ] Creators: keep "Skaragiannis, D." or replace; affiliation
      "Rust-CICFlow (github.com/dr-skaragiannis/Rust-CICFlow)".

## Verify & publish
- [ ] Zenodo computes checksum per uploaded file; cross-check the visible part
      list against `SHA256SUMS*.txt`.
- [ ] Publish → note the DOIs, then add the DOI badge/link into
      `README.md` (Australian section: "Dataset Generation") and to the repo,
      replacing the TODO placeholder.
- [ ] Post-publish: rerun `python scripts/run_experiments.py --data-dir
      experiments/20260923-020154_Local_Computer/subsets --skip-build` on a
      fresh clone to confirm the corpus reproduces the numbers in
      `results_summary_corrected.md`.
